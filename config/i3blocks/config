# i3blocks config file

# List of valid properties:

# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties

command=/usr/share/i3blocks/$BLOCK_NAME
separator_block_width=20
markup=pango
color=#88C0D0

# modules

[keyindicator]
instance=CAPS
interval=once
signal=11
command=~/.config/i3blocks/keyindicator

[volume]
instance=Master
interval=once
command=~/.config/i3blocks/module-volume
signal=10

[module-calendar]
interval=60
command=~/.config/i3blocks/module-calendar

[awsdefault]
label=
command=case $BLOCK_BUTTON in 1|2|3) exec awsdefault-gtk3 ;; esac ; awsdefault is 2>/dev/null ; echo '--No Profile--'
align=left
interval=3
